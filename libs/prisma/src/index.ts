// API pública de la lib: lo que los demás módulos pueden importar
// vía el alias `@app/prisma` (definido en tsconfig.json → paths).
export * from './prisma.module';
export * from './prisma.service';
